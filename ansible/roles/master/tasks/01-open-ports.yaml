---
- name: Abrir los puertos del master
  firewalld:
    port: "{{ item }}"
    state: enabled
    permanent: yes
    immediate: yes
  with_items: "{{ master_ports }}"
  become: yes
  
- name: Recargar Firewall
  shell: firewall-cmd --reload
  become: yes